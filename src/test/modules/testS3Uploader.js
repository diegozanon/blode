var common = require("../common");
var expect = common.expect;
var S3uploader = common.S3uploader;

describe('S3uploader', function() {

    describe('#uploadToS3()', function() {

        it('should upload all files to S3', function() {

        });
    });
});